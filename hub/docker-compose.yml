services:
  app:
    image: henrygd/beszel
    restart: unless-stopped
    volumes:
      - app_data:/beszel_data
    networks:
      proxy_apps:

volumes:
    app_data:

networks:
  proxy_apps:
    name: proxy_apps
    external: true